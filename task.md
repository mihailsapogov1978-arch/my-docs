# TaskFlow API Documentation

Управление списком дел через простой и понятный RESTful API.

---

## 📌 Overview

TaskFlow API позволяет создавать, просматривать, обновлять и удалять задачи в персональном списке дел.  
API использует HTTP-методы и возвращает данные в формате JSON.

- **Базовый URL**: `https://api.taskflow.example/v1`
- **Формат данных**: JSON (`application/json`)
- **Кодировка**: UTF-8
- **Аутентификация**: не требуется в текущей версии (для демо)

> 💡 В продакшен-версии будет использоваться Bearer Token.

---

## 🚀 Quickstart

Получите список всех задач за 30 секунд:

```bash
curl https://api.taskflow.example/v1/tasks
