Последнее обновление: {{ git_revision_date_localized }}


---

## 🅱️ Часть 2: Шаблон для MkDocs (готовый к копированию)

Создайте файл `docs/api/template.md` и используйте его как основу:

```markdown
---
title: "{{ method }} {{ path }}"
description: "{{ краткое_описание }}"
status: stable  # stable | beta | deprecated
---

# `{{ METHOD }} {{ path }}` — {{ Краткое название }}

🟢 Статус: {{ Стабильно/Бета/Устарело }} | 🔄 Обновлено: {{ git_revision_date_localized }}

## 📋 Метаданные

| Поле | Значение | Подтверждение |
|------|----------|---------------|
| **Метод** | `{{ METHOD }}` | ✅ `main.go` |
| **Путь** | `{{ /path }}` | ✅ `main.go` |
| **Обработчик** | `{{ handlers.FuncName }}` | ✅ `main.go` |
| **Авторизация** | {{ ✅ Требуется / ❌ Не требуется }} | {{ источник }} |
| **Параметры пути** | {{ :param1, :param2 / Нет }} | ✅ `main.go` |
| **Query-параметры** | {{ ?limit, ?offset / ⚠️ Неизвестно }} | ❓ Требует проверки |

## 📝 Описание
{{ 1-2 предложения: что делает эндпоинт, зачем нужен }}

> ⚠️ *Если описание предположительное — укажите это*

## 🔧 Параметры запроса

### Параметры пути
| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `:param` | string | ✅ | {{ описание }} |

### Параметры запроса (query)
| Параметр | Тип | Обязательный | Описание | Значение по умолчанию |
|----------|-----|--------------|----------|----------------------|
| `?limit` | integer | ❌ | Количество записей | `20` |

### Тело запроса (JSON)
> *Только для POST/PUT/PATCH*

| Поле | Тип | Обязательный | Описание | Ограничения |
|------|-----|--------------|----------|-------------|
| `field` | string | ✅ | {{ описание }} | {{ мин/макс, формат }} |

## 💻 Пример запроса

```http
{{ METHOD }} {{ /path/with/values }} HTTP/1.1
Host: localhost:8000
{{ Authorization: Bearer <token> }}
{{ Content-Type: application/json }}

{{ {
  "field": "value"
} }}